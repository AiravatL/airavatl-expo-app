# Environment Variables Template for AiravatL
# Copy this to .env.local for local development

# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=your_supabase_url_here
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace the placeholder values with your actual Supabase credentials
# 3. For production builds, use EAS environment variables instead:
#    eas env:set SUPABASE_URL="your_url" --scope project
#    eas env:set SUPABASE_ANON_KEY="your_key" --scope project
