# Environment Variables Template for AiravatL
# Copy this to .env.local for local development

# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=your_supabase_url_here
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# EAS Build Configuration
EAS_PROJECT_ID=your_eas_project_id_here

# Instructions:
# 1. Copy this file to .env.local for local development
# 2. Replace the placeholder values with your actual credentials
# 3. For production builds, set environment variables in EAS:
#    eas env:create --scope project --name EXPO_PUBLIC_SUPABASE_URL --value "your_url"
#    eas env:create --scope project --name EXPO_PUBLIC_SUPABASE_ANON_KEY --value "your_key"
# 4. Upload your FCM server key for push notifications:
#    eas credentials:configure --platform android
# 5. Ensure google-services.json is present locally but never committed to git
